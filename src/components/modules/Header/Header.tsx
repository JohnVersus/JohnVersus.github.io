import { ColorModeButton } from '../../elements';

const Header = () => {
  return (
    <div>
      <section>
        <div>
          <div>
            <ColorModeButton />
          </div>
        </div>
      </section>
    </div>
  );
};

export default Header;
